function double(num) {
    return num * 2;
}

function triple(num) {
    return num * 3;
}

function square(num) {
    return Math.abs(num) * Math.abs(num);
}

export {
    double,
    triple,
    square,
}